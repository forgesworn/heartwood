name: CI

on:
  push:
    branches: [main, "feat/**"]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  # Server-side enforcement of the check-in identity rule: the local pre-commit
  # hook can be bypassed with --no-verify, this gate cannot. The banned string is
  # assembled at runtime so this file stays clean and never trips its own check.
  identity-guard:
    name: identity-guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
      - name: Forbid the banned commit identity
        env:
          BASE: ${{ github.event.pull_request.base.sha }}
          HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          set -eu
          banned="$(printf 'd%s' 'bowles')"
          fail=0
          # 1. No tracked file may contain the banned word (the hook dir may name it).
          if git grep -nIi "$banned" -- . ':(exclude).githooks/'; then
            echo "::error::tracked content contains the banned identity"
            fail=1
          fi
          # 2. No commit in range may be authored/committed by the banned account.
          if [ -n "${BASE:-}" ] && [ -n "${HEAD:-}" ]; then
            range="$BASE..$HEAD"
          else
            range="HEAD~1..HEAD"
          fi
          if git log --format='%an <%ae>%n%cn <%ce>' "$range" | grep -i "$banned"; then
            echo "::error::a commit in $range is authored/committed by the banned identity"
            fail=1
          fi
          [ "$fail" -eq 0 ] && echo "OK: banned identity absent from content and commits ($range)"
          exit "$fail"

  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
      - run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo check --workspace --all-targets

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
      - run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo test --workspace

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          components: clippy
      - run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      - run: cargo clippy --workspace --all-targets -- -D warnings

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  # Supply-chain gate: RUSTSEC advisories, licence allow-list, and source
  # registries, all enforced against deny.toml. Closes the gap where the repo
  # documented `cargo deny check` but never ran it in CI.
  #
  # cargo-deny is installed fresh (not via the cargo-deny-action, whose bundled
  # binary lags and fails to parse newly-published advisory-db entries) so the
  # gate tracks the current advisory schema.
  deny:
    name: cargo-deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: taiki-e/install-action@16b05812d776ae1dfaabc8277e421fb6d2506419 # v2
        with:
          tool: cargo-deny
      - run: cargo deny check advisories licenses sources bans
